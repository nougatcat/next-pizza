#? Переименуй в .env и добавь сюда ключи

# для postgres. Достается из .env.local, если это vercel

# Parameters for Vercel Postgres Templates
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
# если нужно создать локальную бд, то см. гайды и сайт postgres

NEXT_PUBLIC_API_URL=/api
# эту переменную можно потом поменять, если изменится ссылка на апи. Используется для axios.
# Название переменной должно обязательно начинаться с NEXT_PUBLIC

RESEND_API_KEY=
DADATA_API_KEY=
# ! dadata token импортируется из другого места, потому что почему-то отсюда он не работает

YOOKASSA_STORE_ID=
YOOKASSA_API_KEY=
YOOKASSA_CALLBACK_URL='http://localhost:3000/?paid'
# в реальном сайте тут указывается url сайта, а не локалхост, естественно

GITHUB_ID=
GITHUB_SECRET=
# берутся из github->developer settings->oauth

NEXTAUTH_SECRET=
# берется из головы разработчика. Нужно для корректного создания jwt токена

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# берется из console.cloud.google.com/apis/dashboard